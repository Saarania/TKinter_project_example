The executable file can be found in the 'bin' folder.
Files with Python code can be found in the 'files' folder.

The program outputs individual elements (animals). The user has the option to choose a filter to search for elements with a specific property (method of movement).